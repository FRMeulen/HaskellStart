module AssignmentSixDecrypt
	where
		import System.Random
		import System.IO
		import Data.Bits

		--Function to read text file -> monadic
		readTextFile::something -> something
		readTextFile something = something

		--Function to generate list of x random numbers -> monadic
		generateRandomList::Int -> [Int]
		generateRandomList length = [length]

		--Function to decrypt -> non-monadic
		runDecryption::something -> something
		runDecryption something = something

		--Function that combines all three, decrypting a file
		decryptFile::something -> something
		decryptFile something = something