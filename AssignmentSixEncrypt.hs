module AssignmentSixEncrypt
	where
		import System.Random
		import System.IO
		import Data.Bits

		--Function to read text file -> monadic
		readTextFile::something -> something
		readTextFile something = something

		--Function to generate list of x random numbers -> monadic
		generateRandomList::Int -> [Int]
		generateRandomList length = [length]

		--Function to encrypt -> non-monadic
		runEncryption::something -> something
		runEncryption something = something

		--Function that combines all three, encrypting a file
		encryptFile::something -> something
		encryptFile something = something